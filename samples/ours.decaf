class Animal {
  void init(){
    int u;
  }
}

interface Thing {
  void init(int y);
}

class Bird extends Animal implements Thing {
  void print() {
    int x;
  }
  void init(int y) {
    int x;
    int x;
  }
}