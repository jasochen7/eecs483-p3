interface Animal {
  void init();
}

interface Human {
  Animal check(Human h);
}

class Dog implements Animal, Human {
  void init() {
    string name;
    int name;
  }
  Animal check(string h) {
    string x;
    x = h;
  }
}